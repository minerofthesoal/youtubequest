--- a/.github/workflows/build.yml
+++ b/.github/workflows/build.yml
@@ -94,18 +94,31 @@
       - name: Cache QPM dependencies
         uses: actions/cache@v4
         with:
-          # Best-effort cache location for restored packages; run
-          # `qpm config cache path` locally to confirm this on your machine
-          # if restores aren't getting any faster.
-          path: |
-            ~/.local/share/QPM
-            extern
+          # Only cache QPM's own package-download cache (content-addressed,
+          # safe to reuse). Do NOT cache `extern` here: it's restore's
+          # *output*, and letting `restore-keys` fall back to an
+          # older/partial cache entry can hand `qpm restore` a stale
+          # `extern/` it then treats as already-populated, silently
+          # skipping regeneration of extern/extern.cmake. Regenerating
+          # `extern` fresh from this package cache every run is fast and
+          # avoids that failure mode.
+          path: ~/.local/share/QPM
           key: ${{ runner.os }}-qpm-${{ hashFiles('qpm.json', 'qpm.shared.json') }}
           restore-keys: |
             ${{ runner.os }}-qpm-
 
       - name: qpm restore
-        run: qpm restore
+        run: |
+          set -euo pipefail
+          qpm restore
+          # Belt-and-braces: fail loudly here rather than letting a broken
+          # restore surface later as a confusing "header not found" error
+          # deep in compilation. qpm writes these to the repo root, not
+          # into extern/ -- see CMakeLists.txt for why.
+          if [ ! -f extern.cmake ] || [ ! -f qpm_defines.cmake ]; then
+            echo "::error::qpm restore did not produce extern.cmake / qpm_defines.cmake -- dependencies were not restored. Check the qpm restore output above."
+            exit 1
+          fi
 
       - name: Configure + build
         env:

--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -31,15 +31,22 @@
     ${CMAKE_SOURCE_DIR}/extern/includes
 )
 
-# qpm-restored static/shared libs get linked in automatically once you run
-# `qpm restore` -- it drops a qpm_defines.cmake / auto-generated link list
-# into extern/. Keeping this explicit stub here so the project is inspectable
-# without having run qpm yet; delete the placeholder line below once restore
-# has generated the real one.
-if(EXISTS ${CMAKE_SOURCE_DIR}/extern/extern.cmake)
-    include(${CMAKE_SOURCE_DIR}/extern/extern.cmake)
+# qpm restore writes qpm_defines.cmake and extern.cmake to the PROJECT ROOT
+# (next to this file), not into extern/ -- extern/ only holds the downloaded
+# headers and libs. Both are required: qpm_defines.cmake carries
+# restore-generated compile definitions and must be included first (it
+# defines COMPILE_ID/EXTERN_DIR that extern.cmake depends on); extern.cmake
+# carries the include/link setup. Treat either being missing as fatal (not a
+# warning) -- continuing to configure without them just defers the failure
+# to a much more confusing "header not found" error partway through
+# compilation instead of a clear message here.
+if(EXISTS ${CMAKE_SOURCE_DIR}/qpm_defines.cmake)
+    include(${CMAKE_SOURCE_DIR}/qpm_defines.cmake)
+endif()
+if(EXISTS ${CMAKE_SOURCE_DIR}/extern.cmake)
+    include(${CMAKE_SOURCE_DIR}/extern.cmake)
 else()
-    message(WARNING "extern/extern.cmake not found yet -- run 'qpm restore' before building.")
+    message(FATAL_ERROR "extern.cmake not found -- run 'qpm restore' before building.")
 endif()
 
 target_link_libraries(${PROJECT_NAME} PRIVATE log)

--- a/build.sh
+++ b/build.sh
@@ -7,6 +7,11 @@
 
 echo "Restoring qpm dependencies..."
 qpm restore
+if [ ! -f extern.cmake ] || [ ! -f qpm_defines.cmake ]; then
+    echo "error: qpm restore did not produce extern.cmake / qpm_defines.cmake -- dependencies were not restored." >&2
+    echo "If you have a stale ./extern from an old checkout, try: rm -rf extern && qpm restore" >&2
+    exit 1
+fi
 
 mkdir -p build
 cmake -G "Ninja" \

--- a/build.ps1
+++ b/build.ps1
@@ -12,6 +12,10 @@
 
 Write-Host "Restoring qpm dependencies..."
 qpm restore
+if (-not (Test-Path "extern.cmake") -or -not (Test-Path "qpm_defines.cmake")) {
+    Write-Error "qpm restore did not produce extern.cmake / qpm_defines.cmake -- dependencies were not restored. If you have a stale ./extern from an old checkout, try: Remove-Item -Recurse -Force extern; qpm restore"
+    exit 1
+}
 
 $buildDir = "build"
 if (-not (Test-Path $buildDir)) { New-Item -ItemType Directory -Path $buildDir | Out-Null }

--- a/.gitignore
+++ b/.gitignore
@@ -4,6 +4,11 @@
 .vs/
 .vscode/
 
+# Generated by `qpm restore` at the project root (see CMakeLists.txt) --
+# regenerated every restore, so don't commit these.
+/extern.cmake
+/qpm_defines.cmake
+
 # qpm.shared.json is QPM's lockfile (like Cargo.lock / package-lock.json) --
 # it MUST be committed, not ignored. QPM.CLI does an unlocked resolve when
 # it can't find it and prints a warning in CI when that happens. Generate it
